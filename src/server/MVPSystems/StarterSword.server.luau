-- Inicializa o sistema canonico de espadas. O nome antigo do Script foi
-- preservado para manter a estrutura atual do projeto Rojo.

local SwordProgressionService = require(script.Parent:WaitForChild("SwordProgressionService"))

SwordProgressionService.Start()
